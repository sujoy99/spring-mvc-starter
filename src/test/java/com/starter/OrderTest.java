package com.starter;

import com.starter.decorator.decor.ToppingsImpl;
import com.starter.decorator.myPizza.iPizza;
import com.starter.decorator.myPizza.PizzaImpl;
import com.starter.entity.Pizza;
import org.junit.Assert;
import org.junit.jupiter.api.Test;


public class OrderTest {

    @Test
    public void testPizzaApp() {

        /*Pizza pizzaOne = new Pizza(1L, "pizza1", 100.0);
        iPizza pizzaDecor = new PizzaImpl(pizzaOne);
        ToppingsImpl toppings = new ToppingsImpl(pizzaDecor, 30.0);

        Assert.assertEquals("pizza1 (will cost): 130.0",
                String.format("%s (will cost): %s", toppings.getDescription(), toppings.getCost()));*/

    }
}
